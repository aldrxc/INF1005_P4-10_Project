<?php
// =============================================================
// Local database overrides — copy this file to db.local.php
// and edit the values below.
//
//   cp config/db.local.php.example config/db.local.php
//
// db.local.php is listed in .gitignore and will never be
// committed — it is safe to put real passwords here.
// =============================================================

return [
    'host' => 'localhost',   // XAMPP / WAMP default
    'name' => 'merch_vault', // create this DB first: CREATE DATABASE merch_vault;
    'user' => 'root',        // XAMPP default username
    'pass' => '',            // XAMPP default: no password
                             // WAMP/Laragon may use 'root' / '' or 'root' / 'root'
];
